# Server
PORT=4000
CORS_ORIGIN=https://owsbountybot.shelmail.xyz

# Admin token for manual review, program reset, and re-creation
# Demo/testing: admin
BOUNTYBOT_ADMIN_TOKEN=admin

# Database
BOUNTYBOT_DB_PATH=data/bountybot.db

# Demo delay in ms (set to 0 for tests)
BOUNTYBOT_EVALUATION_DELAY_MS=1500

# Treasury source chain — where USDC funds actually live (default: evm)
# Cross-chain payouts (e.g. to Solana) are flagged for bridging
BOUNTYBOT_SOURCE_CHAIN=evm

# OWS vault path (default: ~/.ows/wallets)
OWS_VAULT_PATH=
